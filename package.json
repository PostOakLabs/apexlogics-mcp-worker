{
  "name": "apexlogics-mcp-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "ApexLogics MCP Server — Cloudflare Worker",
  "main": "worker.mjs",
  "scripts": {
    "generate": "node generate.mjs",
    "deploy": "node generate.mjs && npx wrangler deploy",
    "dev": "npx wrangler dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.22.0"
  }
}
